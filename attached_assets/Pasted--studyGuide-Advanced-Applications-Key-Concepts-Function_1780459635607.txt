{
  "studyGuide": "Advanced Applications: Key Concepts - Functions (Class 11 GSEB Mathematics)\n\nNamaste, students! In Class 11 Mathematics, 'Advanced Applications: Key Concepts' primarily delves into the nuanced world of Functions, building upon your foundational understanding. This topic is crucial as functions are the backbone of higher mathematics and scientific modelling.\n\nCore Principles: A function is a special type of relation where every element in the domain (input set) is mapped to exactly one element in the codomain (output set). Understanding this 'one-to-one or many-to-one' mapping is fundamental. We explore how to rigorously define functions and differentiate them from mere relations.\n\nKey Definitions:\n   Domain: The set of all possible input values for which the function is defined.\n   Range: The set of all actual output values produced by the function.\n   Codomain: The set of all possible output values that could be produced.\n   One-One (Injective) Function: Different elements in the domain map to different elements in the codomain.\n   Onto (Surjective) Function: Every element in the codomain has at least one pre-image in the domain.\n   Bijective Function: A function that is both one-one and onto, hence invertible.\n   Composite Function (f o g): Applying one function after another, where the output of the inner function becomes the input of the outer function.\n   Inverse Function (f⁻¹): A function that 'undoes' the effect of the original function, existing only for bijective functions.\n   Special Functions: Understanding properties of the Modulus Function (|x|), Signum Function (sgn(x)), and Greatest Integer Function ([x]) is key.\n\nTechniques and Formulas:\n   Finding Domain: Identify values that make the denominator zero, expressions under square roots negative, or arguments of logarithms non-positive/zero. For trigonometric functions, consider their natural domains.\n   Finding Range: Often involves algebraic manipulation to express 'x' in terms of 'y' or analyzing the graph/behavior of the function.\n   Composite Function (f o g)(x) = f(g(x)): Substitute the expression for g(x) into f(x).\n*   Inverse Function: Set y = f(x), then solve for x in terms of y. Replace x with f⁻¹(y) and then y with x.\n\nReal-life Applications: Functions are everywhere! They describe: the trajectory of a projectile (parabolic function), population growth (exponential function), cost-revenue analysis in business (linear/quadratic functions), and signal processing in electronics. They allow us to model, predict, and understand various phenomena.\n\nRevision Summary: For exams, focus on mastering the techniques for finding domain and range, especially for rational, radical, and special functions. Practice composing and finding inverses of functions. Clearly understand the definitions of one-one, onto, and bijective functions and how to prove them. This topic builds analytical skills essential for future studies.",
  "questions": [
    {
      "id": "q1",
      "type": "MCQ",
      "question": "The domain of the function f(x) = √(x - 2) / (x - 3) is:",
      "options": [
        "A) [2, ∞)",
        "B) (2, ∞) - {3}",
        "C) [2, ∞) - {3}",
        "D) (-∞, 2) ∪ (3, ∞)"
      ],
      "solution": "For the function f(x) = √(x - 2) / (x - 3) to be defined, two conditions must be met:\n1.  The expression under the square root must be non-negative: x - 2 ≥ 0  => x ≥ 2.\n2.  The denominator must not be zero: x - 3 ≠ 0  => x ≠ 3.\n\nCombining these two conditions, we need x to be greater than or equal to 2, but x cannot be 3.\nSo, the domain is [2, ∞) excluding 3, which can be written as [2, ∞) - {3}.\n\nTherefore, the correct option is C.",
      "tip": "Always check both the numerator (especially for roots) and the denominator (for non-zero values) simultaneously when finding the domain of rational functions involving radicals."
    },
    {
      "id": "q2",
      "type": "Short",
      "question": "If f(x) = 3x + 2 and g(x) = x² - 1, find (g o f)(x) and (f o g)(x).",
      "solution": "To find (g o f)(x):\n(g o f)(x) = g(f(x))\nSubstitute f(x) into g(x): g(3x + 2)\nSince g(x) = x² - 1, replace x with (3x + 2):\n(g o f)(x) = (3x + 2)² - 1\n= (9x² + 12x + 4) - 1\n= 9x² + 12x + 3\n\nTo find (f o g)(x):\n(f o g)(x) = f(g(x))\nSubstitute g(x) into f(x): f(x² - 1)\nSince f(x) = 3x + 2, replace x with (x² - 1):\n(f o g)(x) = 3(x² - 1) + 2\n= 3x² - 3 + 2\n= 3x² - 1\n\nKey Grading Keywords: Correct substitution, accurate algebraic expansion, final simplified expressions for both compositions.",
      "tip": "Remember that (f o g)(x) is generally NOT equal to (g o f)(x). Perform each composition step-by-step to avoid errors."
    },
    {
      "id": "q3",
      "type": "Long",
      "question": "Consider the function f: R → R defined by f(x) = (3x - 5) / (2x - 4).\na) Find the domain of f(x).\nb) Show that f is a one-one function.\nc) Find the inverse function f⁻¹(x).",
      "solution": "a) Finding the domain of f(x):\nThe function f(x) = (3x - 5) / (2x - 4) is a rational function. For a rational function to be defined, its denominator must not be equal to zero.\nSet the denominator to zero and solve for x:\n2x - 4 = 0\n2x = 4\nx = 2\nSo, the function is defined for all real numbers except x = 2.\nTherefore, the domain of f(x) is R - {2}.\n\nb) Showing f is a one-one function:\nA function f: A → B is one-one if f(x₁) = f(x₂) implies x₁ = x₂ for all x₁, x₂ ∈ A.\nLet f(x₁) = f(x₂)\n(3x₁ - 5) / (2x₁ - 4) = (3x₂ - 5) / (2x₂ - 4)\nCross-multiply:\n(3x₁ - 5)(2x₂ - 4) = (3x₂ - 5)(2x₁ - 4)\n6x₁x₂ - 12x₁ - 10x₂ + 20 = 6x₁x₂ - 12x₂ - 10x₁ + 20\nSubtract 6x₁x₂ and 20 from both sides:\n-12x₁ - 10x₂ = -12x₂ - 10x₁\nRearrange terms to group x₁ and x₂:\n-12x₁ + 10x₁ = -12x₂ + 10x₂\n-2x₁ = -2x₂\nx₁ = x₂\nSince f(x₁) = f(x₂) implies x₁ = x₂, the function f(x) is a one-one function.\n\nc) Finding the inverse function f⁻¹(x):\nLet y = f(x).\ny = (3x - 5) / (2x - 4)\nOur goal is to express x in terms of y.\ny(2x - 4) = 3x - 5\n2xy - 4y = 3x - 5\nGroup terms with x on one side and terms without x on the other:\n2xy - 3x = 4y - 5\nFactor out x:\nx(2y - 3) = 4y - 5\nx = (4y - 5) / (2y - 3)\nNow, replace x with f⁻¹(y):\nf⁻¹(y) = (4y - 5) / (2y - 3)\nFinally, replace y with x to get the inverse function in terms of x:\nf⁻¹(x) = (4x - 5) / (2x - 3)\n\nImportant note: The domain of f⁻¹(x) is R - {3/2}, which is the range of f(x). To confirm, we must ensure 2x - 3 ≠ 0, so x ≠ 3/2.\n\nKey Grading Keywords: Correct method for domain, algebraic steps for one-one proof, accurate manipulation for inverse, final simplified inverse function.",
      "tip": "For inverse functions, the range of the original function becomes the domain of the inverse. Make sure to clearly show each step of algebraic manipulation, especially when isolating 'x' for the inverse function. For proving one-one, start with f(x₁) = f(x₂) and aim to prove x₁ = x₂."
    }
  ]
}